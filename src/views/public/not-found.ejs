<%- include('../partials/public-header', { pageTitle, pageDescription, site, theme: siteTheme || theme, navigation, currentPath }) %>
<%- include('../partials/hero', { hero, site }) %>

<section class="section">
  <div class="container prose-shell">
    <article class="panel panel--centered">
      <p>未找到相关页面，请检查链接或返回首页继续浏览。</p>
      <% if (site) { %>
        <a href="<%= sitePath('/') %>" class="button button--primary">返回首页</a>
      <% } %>
    </article>
  </div>
</section>

<%- include('../partials/public-footer', { site, theme: siteTheme || theme, navigation, sitePath }) %>
